{
  "name": "tagged-typescript",
  "version": "0.0.1",
  "description": "tagged types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && npx tsc --declaration && npx tslint --project .",
    "run": "npm run build && node ./dist/main.js",
    "clean": "rm -rf dist node_modules"
  },
  "author": "Kim Stebel",
  "license": "Apache-2.0",
  "dependencies": {
    "ts-tagged": "^1.0.0"
  },
  "devDependencies": {
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  }
}
